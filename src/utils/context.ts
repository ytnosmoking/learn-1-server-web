import React from "react";
const info: any = {};
export const TokenContext = React.createContext(info);

const a: Function = () => {};
export const DispatchContext = React.createContext(a);

export const InfoContext = React.createContext(info);
